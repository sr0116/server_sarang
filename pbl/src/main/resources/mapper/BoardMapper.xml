<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="mapper.BoardMapper">
 	<select id="list" resultType="domain.Board">
 	select * from tbl_board 
 	where cno = #{cno}
 	<!-- and title like  concat('%', #{keyword}, '%') -->
 	
 	order by bno desc 
 	limit #{amount} offset #{offset}
 	</select>
 	<select id="selectOne" resultType="board">
 		select * from tbl_board where bno = #{bno}
 	</select>
 	<insert id ="insert">
 	insert into tbl_board( title, content, id, cno) values (#{title}, #{content}, #{id}, #{cno})
  	</insert>
  	
  	<select id="getCount" resultType="long">
  	select count(*) from tbl_board where cno = #{cno} 
  	</select>
  	
  	<!-- 
  	#{var} -> 필요한 형태의 데이터 타입으로 만들어주는 것임
  	${var} -> 사용 하지않기 sql 이 보안이 취약함
  	
  	 -->
 </mapper>